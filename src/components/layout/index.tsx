import { Box } from "@mui/material";

interface ILayoutProps {
    children?: React.ReactNode;
  }

export const Layout: React.FC<ILayoutProps> = ({ children }) => {

    return (
        <Box sx={{color: 'white'}}>fdsfasgfasdgfsdgdfg</Box>
    );
}